sudo apt install -y git curl wget unzip software-properties-common
sudo add-apt-repository ppa:deadsnakes/ppa -y
sudo apt update -y
sudo apt install -y python3.12 python3.12-venv python3.12-dev python3-pip

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo apt install nginx

cd /var/www/

sudo git clone https://github.com/AmitTheCloud/clone-web-app.git

sudo chown -R $USER:$USER clone-web-app

cd /var/www/clone-web-app/nike/backend

python3.12 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

vim .env
MONGO_URL=mongodb://localhost:27017
DB_NAME=nike_db

In your backend file (e.g. server.py or main.py), add this right after app = FastAPI():

@app.get("/")
def read_root():
    return {"message": "Nike Backend API is running 🚀"}

uvicorn server:app --host 0.0.0.0 --port 8000

cd ../frontend
npm install --legacy-peer-deps

rm -rf node_modules package-lock.json

npm install react-scripts@5.0.1 @craco/craco@7.0.0 --save --legacy-peer-deps

npm install ajv@6 ajv-keywords@3 --save-dev --legacy-peer-deps

npm install dotenv --save --legacy-peer-deps

npm install --legacy-peer-deps

npm run build

npm install -g serve
serve -s build
